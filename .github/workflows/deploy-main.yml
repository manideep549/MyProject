name: Salesforce DevOps Pipeline

on:
  push:
    branches:
      - dev
      - uat
      - main

jobs:
  salesforce-ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Install Salesforce CLI
        run: npm install @salesforce/cli --global

      - name: Setup Java (Required for PMD)
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: '11'

      - name: Install PMD
        run: |
          wget https://github.com/pmd/pmd/releases/download/pmd_releases/6.55.0/pmd-bin-6.55.0.zip
          unzip pmd-bin-6.55.0.zip
          echo "$PWD/pmd-bin-6.55.0/bin" >> $GITHUB_PATH

      - name: Authenticate Salesforce Org
        run: |
          echo "${{ secrets.SFDX_AUTH_URL }}" > auth_url.txt
          sf org login sfdx-url -f auth_url.txt -a target_org
          rm auth_url.txt

      - name: Run Apex PMD Scan
        run: |
          pmd-bin-6.55.0/bin/run.sh pmd \
            -d force-app \
            -R category/apex/bestpractices.xml,category/apex/design.xml \
            -f text \
            -minimumpriority 2

      - name: Read Test Classes
        id: tests
        run: |
          # Updated file name to Testclass
          if [ ! -f Testclass ]; then
            echo "Error: Testclass file not found."
            exit 1
          fi
          
          # 1. sed removes Windows line endings (\r)
          # 2. grep removes empty lines
          # 3. xargs/tr creates a clean comma-separated list with no trailing comma
          TESTS=$(sed 's/\r//g' Testclass | grep -v '^$' | xargs | tr ' ' ',')
          
          if [ -z "$TESTS" ]; then
            echo "Error: No test classes found in Testclass"
            exit 1
          fi

          echo "Tests to run: $TESTS"
          echo "test_classes=$TESTS" >> $GITHUB_OUTPUT

      - name: Validate Deployment (DEV / UAT)
        if: github.ref == 'refs/heads/dev' || github.ref == 'refs/heads/uat'
        run: |
          sf project deploy validate \
            --source-dir force-app \
            --test-level RunSpecifiedTests \
            --tests "${{ steps.tests.outputs.test_classes }}" \
            --target-org target_org \
            --wait 30

      - name: Validate Deployment (MAIN)
        if: github.ref == 'refs/heads/main'
        run: |
          sf project deploy validate \
            --source-dir force-app \
            --test-level RunSpecifiedTests \
            --tests "${{ steps.tests.outputs.test_classes }}" \
            --target-org target_org \
            --wait 30

      - name: Run Apex Tests With Coverage
        run: |
          sf apex run test \
            --tests "${{ steps.tests.outputs.test_classes }}" \
            --target-org target_org \
            --wait 30 \
            --code-coverage \
            --result-format json \
            --output-dir test-results

      - name: Enforce 76 Percent Coverage
        run: |
          COVERAGE=$(jq '.summary.orgWideCoverage' test-results/test-result-*.json | tr -d '"%')
          echo "Org Coverage: $COVERAGE%"
          
          if (( $(echo "$COVERAGE < 76" | bc -l) )); then
            echo "Code coverage is $COVERAGE%, which is below the 76% requirement."
            exit 1
          fi

      - name: Deploy to Org (MAIN)
        if: github.ref == 'refs/heads/main'
        run: |
          sf project deploy start \
            --source-dir force-app \
            --test-level RunSpecifiedTests \
            --tests "${{ steps.tests.outputs.test_classes }}" \
            --target-org target_org \
            --wait 30